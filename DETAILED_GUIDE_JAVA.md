# 📚 Подробное пошаговое руководство по Java для начинающих

## 🎯 ВВЕДЕНИЕ

Это руководство — "супер-подробная" версия курса Java для начинающих. Оно сделано в стиле SQL-гайдов с:

- **больше теории** (почему так, а не иначе, как это работает под капотом);
- **больше пошаговых алгоритмов** (сборка программы по шагам, от простого к сложному);
- **больше типовых ошибок** и способов их избежать;
- **чек-листы самопроверки** для каждой задачи.

**Важно:** Это руководство про понимание. Не копируй слепо. Даже если берёшь шаблон из гайда — перепроверь, почему он работает, и попробуй объяснить себе вслух.

---

## 0) Быстрый контекст: про что этот курс

Этот курс — пошаговое изучение Java от основ до продвинутых тем:

- **Уровни 1-2**: Переменные, типы данных, вывод — базовые строительные блоки
- **Уровни 3-4**: Условия и циклы — логика программы
- **Уровни 5-6**: Вложенные циклы и массивы — работа с данными
- **Уровни 7-8**: Методы и строки — организация кода
- **Уровни 9-11**: Классы, объекты, наследование — ООП
- **Уровни 12-16**: Продвинутые темы — коллекции, исключения, файлы

---

## 1) Подготовка к работе (один раз перед началом)

### 1.1. Установка Java

**Проверка установки:**
```bash
java -version
javac -version
```

**Если не установлено:**
- **Windows**: Скачай с oracle.com или используй OpenJDK
- **Linux/WSL**: `sudo apt install openjdk-17-jdk`
- **Mac**: `brew install openjdk@17`

### 1.2. Структура проекта

**Рекомендуемая структура:**
```
lesson/
├── java_projects_tests/
│   ├── zad1/          # Уровень 1
│   ├── zad2/          # Уровень 2
│   ├── zad3/          # Уровень 3
│   └── ...
└── Java_Course_Tasks.md
```

**Или для каждого уровня отдельная папка:**
```
lesson/
├── level1/
│   ├── zad1_1.java
│   └── zad1_2.java
├── level2/
└── ...
```

### 1.3. Запуск программ

**Способ 1: Прямой запуск (Java 11+):**
```bash
java HelloWorld.java
```

**Способ 2: Классический:**
```bash
javac HelloWorld.java
java HelloWorld
```

**Важно:** Имя класса должно совпадать с именем файла!

---

## 2) Уровень 1: Основы (Переменные и вывод)

### 📝 ЗАДАНИЕ 1.1: Вывод имени и возраста

#### 🎯 Цель задания
Создать программу, которая выводит на экран ваше имя и возраст.

#### 📖 Что нужно понять перед началом

**Структура Java программы:**
- Каждая программа начинается с класса
- Внутри класса должен быть метод `main` — точка входа
- `System.out.println()` — вывод на экран

**Почему именно такая структура:**
- Java — объектно-ориентированный язык, всё должно быть в классе
- `main` — специальный метод, с которого JVM начинает выполнение
- `static` — метод принадлежит классу, не объекту (объект создавать не нужно)

#### 🔍 ШАГ 1: Создай базовую структуру

**Алгоритм:**

1. **Создай класс:**
```java
public class zad1_1 {
}
```

2. **Добавь метод main:**
```java
public class zad1_1 {
    public static void main(String[] args) {
    }
}
```

**Объяснение:**
- `public` — метод доступен извне (JVM должна его вызвать)
- `static` — метод класса, не нужен объект
- `void` — метод ничего не возвращает
- `String[] args` — параметры командной строки (пока не используем)

#### 🔍 ШАГ 2: Добавь вывод

**Алгоритм:**

1. **Выведи имя:**
```java
public class zad1_1 {
    public static void main(String[] args) {
        System.out.println("Меня зовут Никита");
    }
}
```

2. **Добавь вывод возраста:**
```java
public class zad1_1 {
    public static void main(String[] args) {
        System.out.println("Меня зовут Никита");
        System.out.println("Мне 23 года");
    }
}
```

**Объяснение `System.out.println()`:**
- `System` — класс из стандартной библиотеки
- `out` — объект для вывода (стандартный поток вывода)
- `println()` — метод, который выводит текст и переходит на новую строку
- `print()` — выводит без перехода на новую строку

#### ✍️ ШАГ 3: Полный код

```java
public class zad1_1 {
    public static void main(String[] args) {
        System.out.println("Меня зовут Никита");
        System.out.println("Мне 23 года");
    }
}
```

#### ⚠️ Типовые ошибки

1. **Ошибка: "class zad1_1 is public, should be declared in a file named zad1_1.java"**
   - **Причина:** Имя класса не совпадает с именем файла
   - **Решение:** Переименуй файл в `zad1_1.java` или класс в имя файла

2. **Ошибка: "cannot find symbol: method println(java.lang.String)"**
   - **Причина:** Опечатка в `println` (например, `printn`)
   - **Решение:** Проверь написание: `println`

3. **Ошибка: "';' expected"**
   - **Причина:** Забыта точка с запятой в конце строки
   - **Решение:** Каждая команда должна заканчиваться `;`

#### ✅ Чек-лист самопроверки

- [ ] Файл называется `zad1_1.java`
- [ ] Класс называется `zad1_1`
- [ ] Есть метод `main` с правильной сигнатурой
- [ ] Используется `System.out.println()`
- [ ] Выводится имя и возраст
- [ ] Программа компилируется без ошибок
- [ ] Программа запускается и выводит правильный результат

#### 🧪 Проверка результата

**Запусти программу:**
```bash
java zad1_1.java
```

**Ожидаемый вывод:**
```
Меня зовут Никита
Мне 23 года
```

---

### 📝 ЗАДАНИЕ 1.2: Переменные для имени, возраста и города

#### 🎯 Цель задания
Создать переменные для хранения имени, возраста и города. Вывести их на экран.

#### 📖 Что нужно понять

**Что такое переменная:**
- Переменная — это именованная ячейка памяти
- В Java нужно указать тип перед именем (статическая типизация)
- Тип определяет, какие данные можно хранить

**Почему нужны переменные:**
- Переиспользование данных
- Изменение значений в одном месте
- Читаемость кода

#### 🔍 ШАГ 1: Создай переменную для имени

**Алгоритм:**

1. **Определи тип:**
   - Имя — это текст → `String`

2. **Создай переменную:**
```java
String name = "Никита";
```

**Объяснение:**
- `String` — тип данных для текста
- `name` — имя переменной (может быть любым, но лучше понятным)
- `= "Никита"` — присваивание значения
- `"Никита"` — строка в двойных кавычках

#### 🔍 ШАГ 2: Добавь переменные для возраста и города

**Алгоритм:**

1. **Возраст — целое число:**
```java
int age = 23;
```

2. **Город — текст:**
```java
String city = "Ярославль";
```

**Объяснение типов:**
- `int` — целые числа (integer)
- `String` — текст (строки)

#### 🔍 ШАГ 3: Выведи переменные

**Алгоритм:**

1. **Выведи имя:**
```java
System.out.println("Имя:" + name);
```

**Объяснение конкатенации:**
- `+` для строк — это объединение (конкатенация)
- `"Имя:" + name` → `"Имя:Никита"`

2. **Добавь вывод остальных:**
```java
System.out.println("Возраст:" + age);
System.out.println("Город:" + city);
```

#### ✍️ ШАГ 4: Полный код

```java
public class zad1_2 {
    public static void main(String[] args) {
        String name = "Никита";
        int age = 23;
        String city = "Ярославль";

        System.out.println("Имя:" + name);
        System.out.println("Возраст:" + age);
        System.out.println("Город:" + city);
    }
}
```

#### ⚠️ Типовые ошибки

1. **Ошибка: "cannot find symbol: variable name"**
   - **Причина:** Переменная объявлена после использования
   - **Решение:** Объявляй переменные перед использованием

2. **Ошибка: "incompatible types: int cannot be converted to String"**
   - **Причина:** Попытка присвоить число строковой переменной
   - **Решение:** Используй правильный тип: `int age = 23;` (не `String age = 23;`)

3. **Ошибка: "unclosed string literal"**
   - **Причина:** Незакрытая кавычка в строке
   - **Решение:** Проверь, что каждая строка начинается и заканчивается кавычками

#### ✅ Чек-лист самопроверки

- [ ] Созданы 3 переменные: `name`, `age`, `city`
- [ ] Правильные типы: `String` для текста, `int` для числа
- [ ] Все переменные выведены на экран
- [ ] Используется конкатенация строк (`+`)
- [ ] Программа компилируется и запускается

---

### 📝 ЗАДАНИЕ 1.4: Арифметические операции

#### 🎯 Цель задания
Создать переменные `a = 10` и `b = 5`. Вывести на экран сумму, разность, произведение и частное.

#### 📖 Что нужно понять

**Арифметические операции:**
- `+` — сложение
- `-` — вычитание
- `*` — умножение
- `/` — деление (для `int` — целочисленное!)
- `%` — остаток от деления

**Важно про деление:**
- `int / int` = `int` (целочисленное деление)
- `10 / 3` = `3` (не 3.33!)
- Для точного деления используй `double`

#### 🔍 ШАГ 1: Создай переменные

**Алгоритм:**

```java
int a = 10;
int b = 5;
```

#### 🔍 ШАГ 2: Вычисли и выведи сумму

**Алгоритм:**

1. **Вычисли сумму:**
```java
int сумма = a + b;
```

2. **Выведи результат:**
```java
System.out.println("Сумма: " + (a + b));
```

**Важно про скобки:**
- `"Сумма: " + a + b` → `"Сумма: 105"` (строки склеиваются!)
- `"Сумма: " + (a + b)` → `"Сумма: 15"` (сначала вычисление, потом конкатенация)

**Почему нужны скобки:**
- Без скобок: `"Сумма: " + a` → `"Сумма: 10"`, затем `+ b` → `"Сумма: 105"`
- Со скобками: `(a + b)` → `15`, затем `"Сумма: " + 15` → `"Сумма: 15"`

#### 🔍 ШАГ 3: Добавь остальные операции

**Алгоритм:**

```java
System.out.println("Сумма: " + (a + b));
System.out.println("Разность: " + (a - b));
System.out.println("Произведение: " + (a * b));
System.out.println("Частное: " + (a / b));
```

**Для вычитания и умножения скобки не обязательны, но не мешают:**
- `"Разность: " + a - b` — ОШИБКА! (нельзя вычитать из строки)
- `"Разность: " + (a - b)` — правильно

#### ✍️ ШАГ 4: Полный код

```java
public class zad1_4 {
    public static void main(String[] args) {
        int a = 10;
        int b = 5;

        System.out.println("Сумма: " + (a + b));
        System.out.println("Разность: " + (a - b));
        System.out.println("Произведение: " + (a * b));
        System.out.println("Частное: " + (a / b));
    }
}
```

#### ⚠️ Типовые ошибки

1. **Ошибка: "bad operand types for binary operator '-'"**
   - **Причина:** `"Разность: " + a - b` — пытаешься вычесть число из строки
   - **Решение:** Используй скобки: `"Разность: " + (a - b)`

2. **Результат "Сумма: 105" вместо "Сумма: 15"**
   - **Причина:** Нет скобок, строки склеиваются
   - **Решение:** `"Сумма: " + (a + b)`

3. **Частное получается не то (например, 7/3 = 2 вместо 2.33)**
   - **Причина:** Целочисленное деление для `int`
   - **Решение:** Если нужна точность, используй `double`: `double a = 10.0; double b = 3.0;`

#### ✅ Чек-лист самопроверки

- [ ] Переменные `a` и `b` созданы и инициализированы
- [ ] Все 4 операции выведены
- [ ] Для сложения используются скобки (чтобы избежать склеивания строк)
- [ ] Результаты правильные: 15, 5, 50, 2
- [ ] Программа компилируется и запускается

---

## 3) Уровень 2: Типы данных

### 📝 ЗАДАНИЕ 2.1: Переменные разных типов

#### 🎯 Цель задания
Создать переменные разных типов: `int`, `double`, `boolean`, `char`. Вывести все переменные на экран.

#### 📖 Что нужно понять

**Примитивные типы в Java:**
- `int` — целые числа (4 байта, от -2 млрд до +2 млрд)
- `double` — дробные числа (8 байт, ~15 знаков точности)
- `boolean` — логические значения (только `true` или `false`)
- `char` — один символ (2 байта, Unicode)

**Почему разные типы:**
- Экономия памяти
- Безопасность типов (нельзя случайно смешать типы)
- Явность намерений

#### 🔍 ШАГ 1: Создай переменную типа int

**Алгоритм:**

```java
int число = 42;
```

**Объяснение:**
- `int` — тип для целых чисел
- `число` — имя переменной (можно использовать кириллицу, но лучше латиницу)
- `42` — значение (без кавычек, это число)

#### 🔍 ШАГ 2: Создай переменную типа double

**Алгоритм:**

```java
double десятичное = 3.14;
```

**Объяснение:**
- `double` — тип для дробных чисел
- `3.14` — значение с точкой (не запятой!)

**Важно:**
- В Java используется точка для десятичных чисел (не запятая!)
- `3,14` — это ошибка
- `3.14` — правильно

#### 🔍 ШАГ 3: Создай переменную типа boolean

**Алгоритм:**

```java
boolean правда = true;
```

**Объяснение:**
- `boolean` — логический тип
- `true` или `false` — только два возможных значения
- Без кавычек! `"true"` — это строка, `true` — логическое значение

**Важно:**
- В Java нельзя использовать числа вместо boolean (в отличие от C)
- `if (1)` — ОШИБКА!
- `if (true)` — правильно

#### 🔍 ШАГ 4: Создай переменную типа char

**Алгоритм:**

```java
char символ = 'A';
```

**Объяснение:**
- `char` — один символ
- Одинарные кавычки! `'A'` — правильно, `"A"` — это строка

**Разница:**
- `char символ = 'A';` — один символ
- `String строка = "A";` — строка из одного символа

#### 🔍 ШАГ 5: Выведи все переменные

**Алгоритм:**

```java
System.out.println("Число: " + число);
System.out.println("Десятичное число: " + десятичное);
System.out.println("Логическое значение: " + правда);
System.out.println("Символ: " + символ);
```

**Объяснение:**
- При конкатенации с `+` все автоматически преобразуется в строку
- `число` (int) → строка "42"
- `десятичное` (double) → строка "3.14"
- `правда` (boolean) → строка "true"
- `символ` (char) → строка "A"

#### ✍️ ШАГ 6: Полный код

```java
public class zad2_1 {
    public static void main(String[] args) {
        int число = 42;
        double десятичное = 3.14;
        boolean правда = true;
        char символ = 'A';

        System.out.println("Число: " + число);
        System.out.println("Десятичное число: " + десятичное);
        System.out.println("Логическое значение: " + правда);
        System.out.println("Символ: " + символ);
    }
}
```

#### ⚠️ Типовые ошибки

1. **Ошибка: "unclosed character literal"**
   - **Причина:** Использованы двойные кавычки для `char`: `char c = "A";`
   - **Решение:** Используй одинарные: `char c = 'A';`

2. **Ошибка: "incompatible types: String cannot be converted to boolean"**
   - **Причина:** `boolean b = "true";` (строка вместо логического значения)
   - **Решение:** `boolean b = true;` (без кавычек)

3. **Ошибка: "';' expected"**
   - **Причина:** Запятая вместо точки в `double`: `double x = 3,14;`
   - **Решение:** Используй точку: `double x = 3.14;`

#### ✅ Чек-лист самопроверки

- [ ] Созданы 4 переменные разных типов
- [ ] `int` — целое число без кавычек
- [ ] `double` — дробное число с точкой
- [ ] `boolean` — `true` или `false` без кавычек
- [ ] `char` — один символ в одинарных кавычках
- [ ] Все переменные выведены на экран
- [ ] Программа компилируется и запускается

---

## 4) Уровень 3: Условные операторы (if/else)

### 📝 ЗАДАНИЕ 3.1: Проверка возраста

#### 🎯 Цель задания
Создать переменную `age = 18`. Если возраст >= 18, вывести "Совершеннолетний", иначе "Несовершеннолетний".

#### 📖 Что нужно понять

**Что такое условный оператор:**
- Позволяет выполнять разный код в зависимости от условия
- Основа логики программирования

**Синтаксис if-else:**
```java
if (условие) {
    // код, если условие true
} else {
    // код, если условие false
}
```

**Операторы сравнения:**
- `==` — равно (НЕ `=`, это присваивание!)
- `!=` — не равно
- `>` — больше
- `<` — меньше
- `>=` — больше или равно
- `<=` — меньше или равно

#### 🔍 ШАГ 1: Создай переменную age

**Алгоритм:**

```java
int age = 18;
```

#### 🔍 ШАГ 2: Напиши условие if

**Алгоритм:**

1. **Начни с if:**
```java
if (age >= 18) {
}
```

**Объяснение:**
- `if` — ключевое слово
- `(age >= 18)` — условие в скобках
- `>=` — "больше или равно"
- `{ }` — блок кода, который выполнится, если условие `true`

2. **Добавь код внутри if:**
```java
if (age >= 18) {
    System.out.println("Совершеннолетний");
}
```

#### 🔍 ШАГ 3: Добавь else

**Алгоритм:**

```java
if (age >= 18) {
    System.out.println("Совершеннолетний");
} else {
    System.out.println("Несовершеннолетний");
}
```

**Объяснение:**
- `else` — блок, который выполнится, если условие `false`
- Не требует условия (это "во всех остальных случаях")

#### ✍️ ШАГ 4: Полный код

```java
public class zad3_1 {
    public static void main(String[] args) {
        int age = 18;

        if (age >= 18) {
            System.out.println("Совершеннолетний");
        } else {
            System.out.println("Несовершеннолетний");
        }
    }
}
```

#### ⚠️ Типовые ошибки

1. **Ошибка: "';' expected"**
   - **Причина:** `if age >= 18` (нет скобок вокруг условия)
   - **Решение:** `if (age >= 18)`

2. **Ошибка: "incompatible types: int cannot be converted to boolean"**
   - **Причина:** `if (age = 18)` (присваивание вместо сравнения)
   - **Решение:** `if (age == 18)` или `if (age >= 18)`

3. **Ошибка: "cannot find symbol: variable age"**
   - **Причина:** Переменная объявлена после использования
   - **Решение:** Объявляй переменные перед использованием

4. **Логическая ошибка: всегда выводит "Совершеннолетний"**
   - **Причина:** `if (age = 18)` — это присваивание, всегда возвращает 18 (true)
   - **Решение:** Используй `==` для сравнения

#### ✅ Чек-лист самопроверки

- [ ] Переменная `age` создана
- [ ] Используется `if` с условием в скобках
- [ ] Используется `>=` (не `=`)
- [ ] Есть блок `else`
- [ ] Для `age = 18` выводится "Совершеннолетний"
- [ ] Для `age = 16` выводится "Несовершеннолетний"
- [ ] Программа компилируется и запускается

---

### 📝 ЗАДАНИЕ 3.2: Температура (множественные условия)

#### 🎯 Цель задания
Создать переменную `temperature = 25`. Вывести:
- "Жарко" если температура > 30
- "Тепло" если температура от 20 до 30
- "Холодно" если температура < 20

#### 📖 Что нужно понять

**Множественные условия:**
- `if-else if-else` — цепочка условий
- Проверка идёт сверху вниз
- Как только найдено истинное условие, остальные пропускаются

**Логика проверки:**
- Сначала проверяем самое строгое условие (> 30)
- Затем среднее (20-30)
- В конце — всё остальное (< 20)

#### 🔍 ШАГ 1: Создай переменную

**Алгоритм:**

```java
int temperature = 25;
```

#### 🔍 ШАГ 2: Напиши первое условие (жарко)

**Алгоритм:**

```java
if (temperature > 30) {
    System.out.println("Жарко");
}
```

**Объяснение:**
- `> 30` — строго больше 30
- Если температура 31, 35, 40 — выведет "Жарко"

#### 🔍 ШАГ 3: Добавь else if для "Тепло"

**Алгоритм:**

```java
if (temperature > 30) {
    System.out.println("Жарко");
} else if (temperature >= 20 && temperature <= 30) {
    System.out.println("Тепло");
}
```

**Объяснение:**
- `else if` — следующее условие в цепочке
- `temperature >= 20 && temperature <= 30` — от 20 до 30 включительно
- `&&` — логическое И (оба условия должны быть true)

**Альтернативный вариант (проще):**
```java
} else if (temperature >= 20) {
    System.out.println("Тепло");
}
```
- Если мы дошли до этого `else if`, значит `temperature <= 30` (иначе сработал бы первый if)
- Поэтому достаточно проверить `>= 20`

#### 🔍 ШАГ 4: Добавь else для "Холодно"

**Алгоритм:**

```java
if (temperature > 30) {
    System.out.println("Жарко");
} else if (temperature >= 20) {
    System.out.println("Тепло");
} else {
    System.out.println("Холодно");
}
```

**Объяснение:**
- `else` — всё остальное (температура < 20)
- Не требует условия

#### ✍️ ШАГ 5: Полный код

```java
public class zad3_2 {
    public static void main(String[] args) {
        int temperature = 25;

        if (temperature > 30) {
            System.out.println("Жарко");
        } else if (temperature >= 20) {
            System.out.println("Тепло");
        } else {
            System.out.println("Холодно");
        }
    }
}
```

#### ⚠️ Типовые ошибки

1. **Ошибка: выводит и "Тепло" и "Холодно"**
   - **Причина:** Использованы отдельные `if` вместо `else if`
   - **Решение:** Используй `else if` для связанных условий

2. **Ошибка: граничные случаи (20, 30) обрабатываются неправильно**
   - **Причина:** Неправильные операторы (`>` вместо `>=`)
   - **Решение:** Проверь границы: `>= 20` включает 20

3. **Ошибка: "bad operand types for binary operator '&&'"**
   - **Причина:** `temperature >= 20 && <= 30` (нет переменной во второй части)
   - **Решение:** `temperature >= 20 && temperature <= 30`

#### ✅ Чек-лист самопроверки

- [ ] Переменная `temperature` создана
- [ ] Используется `if-else if-else`
- [ ] Границы правильные: > 30, >= 20, < 20
- [ ] Для 25 выводится "Тепло"
- [ ] Для 35 выводится "Жарко"
- [ ] Для 15 выводится "Холодно"
- [ ] Граничные значения (20, 30) обрабатываются правильно

---

### 📝 ЗАДАНИЕ 3.5: Проверка четности числа

#### 🎯 Цель задания
Создать переменную `number = 7`. Проверьте, является ли число четным или нечетным. Вывести результат.

#### 📖 Что нужно понять

**Что такое четное число:**
- Четное число делится на 2 без остатка
- Остаток от деления на 2 = 0
- Нечетное число: остаток = 1

**Оператор `%` (модуло):**
- `a % b` — остаток от деления `a` на `b`
- `7 % 2` = 1 (7 = 3*2 + 1)
- `8 % 2` = 0 (8 = 4*2 + 0)

**Проверка четности:**
- `number % 2 == 0` — четное
- `number % 2 != 0` — нечетное (или `number % 2 == 1`)

#### 🔍 ШАГ 1: Создай переменную

**Алгоритм:**

```java
int number = 7;
```

#### 🔍 ШАГ 2: Напиши условие для четного числа

**Алгоритм:**

```java
if (number % 2 == 0) {
    System.out.println("Число " + number + " четное");
}
```

**Объяснение:**
- `number % 2` — остаток от деления на 2
- `== 0` — проверка, что остаток равен 0
- Если остаток 0 → число четное

**Пошагово для number = 7:**
- `7 % 2` = 1
- `1 == 0` → `false`
- Условие не выполняется

**Пошагово для number = 8:**
- `8 % 2` = 0
- `0 == 0` → `true`
- Условие выполняется

#### 🔍 ШАГ 3: Добавь else для нечетного

**Алгоритм:**

```java
if (number % 2 == 0) {
    System.out.println("Число " + number + " четное");
} else {
    System.out.println("Число " + number + " нечетное");
}
```

**Объяснение:**
- Если число не четное (остаток не 0), значит оно нечетное
- `else` покрывает все остальные случаи

#### ✍️ ШАГ 4: Полный код

```java
public class zad3_5 {
    public static void main(String[] args) {
        int number = 7;

        if (number % 2 == 0) {
            System.out.println("Число " + number + " четное");
        } else {
            System.out.println("Число " + number + " нечетное");
        }
    }
}
```

#### ⚠️ Типовые ошибки

1. **Ошибка: "bad operand types for binary operator '%'"**
   - **Причина:** `number % "2"` (строка вместо числа)
   - **Решение:** `number % 2` (число без кавычек)

2. **Ошибка: всегда выводит "четное"**
   - **Причина:** `number % 2 = 0` (присваивание вместо сравнения)
   - **Решение:** `number % 2 == 0` (двойное равно для сравнения)

3. **Ошибка: "operator % cannot be applied to String"**
   - **Причина:** `number` объявлен как `String`
   - **Решение:** Используй `int number = 7;`

#### ✅ Чек-лист самопроверки

- [ ] Переменная `number` создана
- [ ] Используется `number % 2 == 0` для проверки
- [ ] Для четных чисел (2, 4, 6, 8) выводится "четное"
- [ ] Для нечетных чисел (1, 3, 5, 7) выводится "нечетное"
- [ ] Для 0 выводится "четное" (0 % 2 = 0)
- [ ] Программа компилируется и запускается

---

## 5) Уровень 4: Циклы (for, while)

### 📝 ЗАДАНИЕ 4.1: Вывод чисел от 1 до 10

#### 🎯 Цель задания
Вывести числа от 1 до 10 используя цикл `for`.

#### 📖 Что нужно понять

**Что такое цикл:**
- Позволяет выполнить код несколько раз
- Основа автоматизации

**Синтаксис for:**
```java
for (начало; условие; шаг) {
    // код
}
```

**Как работает:**
1. Выполняется "начало" (один раз)
2. Проверяется "условие"
3. Если `true` → выполняется код в `{ }`
4. Выполняется "шаг"
5. Возврат к шагу 2

#### 🔍 ШАГ 1: Создай базовый цикл

**Алгоритм:**

1. **Начни с переменной счетчика:**
```java
for (int i = 1; ; ) {
}
```

2. **Добавь условие (продолжать пока i <= 10):**
```java
for (int i = 1; i <= 10; ) {
}
```

3. **Добавь шаг (увеличить i на 1):**
```java
for (int i = 1; i <= 10; i++) {
}
```

**Объяснение:**
- `int i = 1` — начальное значение (выполняется один раз)
- `i <= 10` — условие (проверяется перед каждой итерацией)
- `i++` — увеличить i на 1 (выполняется после каждой итерации)

#### 🔍 ШАГ 2: Добавь вывод в цикл

**Алгоритм:**

```java
for (int i = 1; i <= 10; i++) {
    System.out.println(i);
}
```

**Объяснение:**
- `System.out.println(i)` — выводит значение `i`
- На каждой итерации `i` увеличивается на 1

**Последовательность выполнения:**
```
Итерация 1: i=1, проверка 1<=10 (true), вывод 1, i становится 2
Итерация 2: i=2, проверка 2<=10 (true), вывод 2, i становится 3
...
Итерация 10: i=10, проверка 10<=10 (true), вывод 10, i становится 11
Итерация 11: i=11, проверка 11<=10 (false), цикл завершается
```

#### ✍️ ШАГ 3: Полный код

```java
public class zad4_1 {
    public static void main(String[] args) {
        for (int i = 1; i <= 10; i++) {
            System.out.println(i);
        }
    }
}
```

#### ⚠️ Типовые ошибки

1. **Ошибка: бесконечный цикл**
   - **Причина:** Забыт шаг `i++` или условие всегда `true`
   - **Решение:** Всегда проверяй, что условие когда-нибудь станет `false`

2. **Ошибка: выводит числа от 0 до 9**
   - **Причина:** `for (int i = 0; i < 10; i++)` (начинается с 0, условие `< 10`)
   - **Решение:** `for (int i = 1; i <= 10; i++)`

3. **Ошибка: "cannot find symbol: variable i"**
   - **Причина:** Попытка использовать `i` вне цикла
   - **Решение:** `i` видна только внутри цикла `for`

#### ✅ Чек-лист самопроверки

- [ ] Используется цикл `for`
- [ ] Начальное значение: `i = 1`
- [ ] Условие: `i <= 10`
- [ ] Шаг: `i++`
- [ ] Выводятся числа от 1 до 10
- [ ] Каждое число на новой строке
- [ ] Программа компилируется и запускается

---

### 📝 ЗАДАНИЕ 4.4: Сумма чисел от 1 до 100

#### 🎯 Цель задания
Вычислить сумму чисел от 1 до 100.

#### 📖 Что нужно понять

**Накопление суммы:**
- Нужна переменная для хранения суммы
- Начальное значение: 0
- На каждой итерации добавляем текущее число к сумме

**Паттерн накопления:**
```java
int сумма = 0;  // начальное значение
for (int i = 1; i <= 100; i++) {
    сумма = сумма + i;  // или сумма += i
}
```

#### 🔍 ШАГ 1: Создай переменную для суммы

**Алгоритм:**

```java
int сумма = 0;
```

**Объяснение:**
- Начальное значение 0 (нейтральный элемент для сложения)
- Будет накапливать сумму по мере прохождения цикла

#### 🔍 ШАГ 2: Создай цикл от 1 до 100

**Алгоритм:**

```java
for (int i = 1; i <= 100; i++) {
}
```

#### 🔍 ШАГ 3: Добавь накопление суммы

**Алгоритм:**

```java
int сумма = 0;
for (int i = 1; i <= 100; i++) {
    сумма = сумма + i;
}
```

**Объяснение:**
- `сумма = сумма + i` — берем текущую сумму и добавляем i
- На первой итерации: `сумма = 0 + 1 = 1`
- На второй: `сумма = 1 + 2 = 3`
- На третьей: `сумма = 3 + 3 = 6`
- И так далее...

**Сокращенная запись:**
```java
сумма += i;  // то же самое, что сумма = сумма + i
```

#### 🔍 ШАГ 4: Выведи результат

**Алгоритм:**

```java
int сумма = 0;
for (int i = 1; i <= 100; i++) {
    сумма += i;
}
System.out.println("Сумма чисел от 1 до 100: " + сумма);
```

**Важно:** Вывод должен быть ПОСЛЕ цикла, не внутри!

#### ✍️ ШАГ 5: Полный код

```java
public class zad4_4 {
    public static void main(String[] args) {
        int сумма = 0;
        for (int i = 1; i <= 100; i++) {
            сумма += i;
        }
        System.out.println("Сумма чисел от 1 до 100: " + сумма);
    }
}
```

#### ⚠️ Типовые ошибки

1. **Ошибка: сумма всегда равна 0**
   - **Причина:** `сумма` объявлена внутри цикла: `for (...) { int сумма = 0; }`
   - **Решение:** Объявляй `сумма` ДО цикла

2. **Ошибка: выводит каждое число вместо суммы**
   - **Причина:** `System.out.println(i)` внутри цикла вместо `System.out.println(сумма)` после цикла
   - **Решение:** Вывод должен быть после цикла

3. **Ошибка: переполнение (для больших сумм)**
   - **Причина:** `int` может хранить только до ~2 млрд
   - **Решение:** Для больших чисел используй `long`: `long сумма = 0;`

#### ✅ Чек-лист самопроверки

- [ ] Переменная `сумма` создана ДО цикла
- [ ] Начальное значение: `сумма = 0`
- [ ] Цикл от 1 до 100
- [ ] На каждой итерации: `сумма += i`
- [ ] Вывод ПОСЛЕ цикла
- [ ] Результат: 5050
- [ ] Программа компилируется и запускается

---

## 6) Уровень 5: Вложенные циклы

### 📝 ЗАДАНИЕ 5.1: Прямоугольник из звездочек

#### 🎯 Цель задания
Вывести прямоугольник из звездочек размером 5x5.

#### 📖 Что нужно понять

**Вложенные циклы:**
- Цикл внутри другого цикла
- Внешний цикл — строки
- Внутренний цикл — столбцы (символы в строке)

**Логика:**
- Для каждой строки (внешний цикл)
- Вывести 5 звездочек (внутренний цикл)
- Перейти на новую строку

#### 🔍 ШАГ 1: Создай внешний цикл (строки)

**Алгоритм:**

```java
for (int i = 0; i < 5; i++) {
    // здесь будет внутренний цикл
}
```

**Объяснение:**
- `i` — номер строки (0, 1, 2, 3, 4)
- 5 итераций = 5 строк

#### 🔍 ШАГ 2: Добавь внутренний цикл (столбцы)

**Алгоритм:**

```java
for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 5; j++) {
        System.out.print("*");
    }
}
```

**Объяснение:**
- `j` — номер столбца (0, 1, 2, 3, 4)
- `System.out.print("*")` — выводит звездочку БЕЗ перехода на новую строку
- Для каждой строки выводится 5 звездочек

#### 🔍 ШАГ 3: Добавь переход на новую строку

**Алгоритм:**

```java
for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 5; j++) {
        System.out.print("*");
    }
    System.out.println();  // переход на новую строку после каждой строки
}
```

**Объяснение:**
- `System.out.println()` — переходит на новую строку
- Выполняется после внутреннего цикла (после вывода всех звездочек строки)

**Последовательность выполнения:**
```
i=0: j=0,1,2,3,4 (выводит *****), затем println (новая строка)
i=1: j=0,1,2,3,4 (выводит *****), затем println (новая строка)
...
```

#### ✍️ ШАГ 4: Полный код

```java
public class zad5_1 {
    public static void main(String[] args) {
        for (int i = 0; i < 5; i++) {
            for (int j = 0; j < 5; j++) {
                System.out.print("*");
            }
            System.out.println();
        }
    }
}
```

#### ⚠️ Типовые ошибки

1. **Ошибка: все звездочки в одну строку**
   - **Причина:** Забыт `System.out.println()` после внутреннего цикла
   - **Решение:** Добавь `System.out.println();` после внутреннего цикла

2. **Ошибка: только одна строка**
   - **Причина:** `System.out.println("*")` внутри внутреннего цикла (переход на новую строку после каждой звездочки)
   - **Решение:** Используй `System.out.print("*")` внутри, `println()` снаружи

3. **Ошибка: прямоугольник неправильного размера**
   - **Причина:** Неправильные границы циклов
   - **Решение:** Оба цикла должны быть `i < 5` и `j < 5` для размера 5x5

#### ✅ Чек-лист самопроверки

- [ ] Два вложенных цикла
- [ ] Внешний цикл: строки (5 итераций)
- [ ] Внутренний цикл: столбцы (5 итераций)
- [ ] Внутри внутреннего цикла: `System.out.print("*")`
- [ ] После внутреннего цикла: `System.out.println()`
- [ ] Результат: 5 строк по 5 звездочек
- [ ] Программа компилируется и запускается

---

### 📝 ЗАДАНИЕ 5.2: Треугольник из звездочек

#### 🎯 Цель задания
Вывести треугольник из звездочек:
```
*
**
***
****
*****
```

#### 📖 Что нужно понять

**Логика треугольника:**
- Строка 1: 1 звездочка
- Строка 2: 2 звездочки
- Строка 3: 3 звездочки
- ...
- Строка i: i звездочек

**Формула:**
- Количество звездочек = номер строки
- Если строки нумеруются с 1: `j <= i`
- Если строки нумеруются с 0: `j <= i` (но тогда первая строка будет 0 звездочек!)

#### 🔍 ШАГ 1: Создай внешний цикл (строки с 1)

**Алгоритм:**

```java
for (int i = 1; i <= 5; i++) {
}
```

**Объяснение:**
- Начинаем с 1 (первая строка)
- `i <= 5` — 5 строк
- `i` — номер строки (1, 2, 3, 4, 5)

#### 🔍 ШАГ 2: Добавь внутренний цикл (количество = номер строки)

**Алгоритм:**

```java
for (int i = 1; i <= 5; i++) {
    for (int j = 1; j <= i; j++) {
        System.out.print("*");
    }
    System.out.println();
}
```

**Объяснение:**
- `j <= i` — количество звездочек = номер строки
- Для i=1: j от 1 до 1 → 1 звездочка
- Для i=2: j от 1 до 2 → 2 звездочки
- Для i=3: j от 1 до 3 → 3 звездочки
- И так далее...

#### ✍️ ШАГ 3: Полный код

```java
public class zad5_2 {
    public static void main(String[] args) {
        for (int i = 1; i <= 5; i++) {
            for (int j = 1; j <= i; j++) {
                System.out.print("*");
            }
            System.out.println();
        }
    }
}
```

#### ⚠️ Типовые ошибки

1. **Ошибка: все строки одинаковой длины**
   - **Причина:** `j <= 5` вместо `j <= i`
   - **Решение:** Количество звездочек должно зависеть от `i`

2. **Ошибка: первая строка пустая**
   - **Причина:** Начали с `i = 0`, тогда `j <= 0` не выполнится
   - **Решение:** Начинай с `i = 1`

3. **Ошибка: треугольник перевернут**
   - **Причина:** `j <= (6 - i)` вместо `j <= i`
   - **Решение:** Используй `j <= i` для растущего треугольника

#### ✅ Чек-лист самопроверки

- [ ] Внешний цикл от 1 до 5
- [ ] Внутренний цикл: `j <= i`
- [ ] Количество звездочек увеличивается с каждой строкой
- [ ] Результат: треугольник из 5 строк
- [ ] Программа компилируется и запускается

---

### 📝 ЗАДАНИЕ 5.5: Ромб из звездочек

#### 🎯 Цель задания
Вывести ромб из звездочек (размер 5):
```
  *
 ***
*****
 ***
  *
```

#### 📖 Что нужно понять

**Структура ромба:**
- Верхняя часть: растущий треугольник
- Нижняя часть: убывающий треугольник
- Нужны пробелы для центрирования

**Логика для размера 5:**
- Строка 0: 2 пробела, 1 звездочка
- Строка 1: 1 пробел, 3 звездочки
- Строка 2: 0 пробелов, 5 звездочек (центр)
- Строка 3: 1 пробел, 3 звездочки
- Строка 4: 2 пробела, 1 звездочка

**Формулы:**
- Центр: `center = size / 2` (для размера 5: center = 2)
- Пробелы: `center - i` (для верхней части)
- Звездочки: `2 * i + 1` (для верхней части)

#### 🔍 ШАГ 1: Определи центр

**Алгоритм:**

```java
int size = 5;
int center = size / 2;  // 2
```

**Объяснение:**
- Центр — это строка с максимальным количеством звездочек
- Для размера 5: центр на строке 2 (индекс, начиная с 0)

#### 🔍 ШАГ 2: Создай верхнюю часть (включая центр)

**Алгоритм:**

```java
int size = 5;
int center = size / 2;

// Верхняя часть + центр
for (int i = 0; i <= center; i++) {
    // пробелы
    for (int j = 0; j < center - i; j++) {
        System.out.print(" ");
    }
    // звездочки
    for (int j = 0; j < 2 * i + 1; j++) {
        System.out.print("*");
    }
    System.out.println();
}
```

**Объяснение:**
- `i` от 0 до center (0, 1, 2)
- Пробелы: `center - i` (2, 1, 0)
- Звездочки: `2 * i + 1` (1, 3, 5)

#### 🔍 ШАГ 3: Добавь нижнюю часть

**Алгоритм:**

```java
// Нижняя часть
for (int i = center - 1; i >= 0; i--) {
    // пробелы
    for (int j = 0; j < center - i; j++) {
        System.out.print(" ");
    }
    // звездочки
    for (int j = 0; j < 2 * i + 1; j++) {
        System.out.print("*");
    }
    System.out.println();
}
```

**Объяснение:**
- `i` от `center - 1` до 0 (1, 0) — обратный порядок
- Те же формулы для пробелов и звездочек
- Результат: симметричная нижняя часть

#### ✍️ ШАГ 4: Полный код

```java
public class zad5_5 {
    public static void main(String[] args) {
        int size = 5;
        int center = size / 2;
        
        // Верхняя часть + центр
        for (int i = 0; i <= center; i++) {
            // Пробелы
            for (int j = 0; j < center - i; j++) {
                System.out.print(" ");
            }
            // Звездочки
            for (int j = 0; j < 2 * i + 1; j++) {
                System.out.print("*");
            }
            System.out.println();
        }
        
        // Нижняя часть
        for (int i = center - 1; i >= 0; i--) {
            // Пробелы
            for (int j = 0; j < center - i; j++) {
                System.out.print(" ");
            }
            // Звездочки
            for (int j = 0; j < 2 * i + 1; j++) {
                System.out.print("*");
            }
            System.out.println();
        }
    }
}
```

#### ⚠️ Типовые ошибки

1. **Ошибка: ромб несимметричный**
   - **Причина:** Неправильные формулы для пробелов/звездочек в нижней части
   - **Решение:** Используй те же формулы, что и для верхней части

2. **Ошибка: центр дублируется**
   - **Причина:** Верхняя часть включает центр, нижняя тоже начинается с center
   - **Решение:** Нижняя часть должна начинаться с `center - 1`

3. **Ошибка: пробелы не выводятся**
   - **Причина:** Забыт цикл для пробелов
   - **Решение:** Нужны два внутренних цикла: один для пробелов, один для звездочек

#### ✅ Чек-лист самопроверки

- [ ] Определен центр: `center = size / 2`
- [ ] Верхняя часть: от 0 до center
- [ ] Нижняя часть: от center-1 до 0 (обратный порядок)
- [ ] Пробелы: `center - i`
- [ ] Звездочки: `2 * i + 1`
- [ ] Результат: симметричный ромб
- [ ] Программа компилируется и запускается

---

## 7) Уровень 6: Массивы

### 📝 ЗАДАНИЕ 6.1: Вывод элементов массива

#### 🎯 Цель задания
Создать массив из 5 чисел: {1, 2, 3, 4, 5}. Вывести все элементы массива.

#### 📖 Что нужно понять

**Что такое массив:**
- Структура данных для хранения нескольких значений одного типа
- Фиксированный размер
- Индексация с 0

**Создание массива:**
```java
int[] числа = {1, 2, 3, 4, 5};
// или
int[] числа = new int[5];
```

**Доступ к элементам:**
- `массив[0]` — первый элемент
- `массив[4]` — последний элемент (для массива из 5 элементов)
- `массив.length` — длина массива (5)

#### 🔍 ШАГ 1: Создай массив

**Алгоритм:**

```java
int[] числа = {1, 2, 3, 4, 5};
```

**Объяснение:**
- `int[]` — тип: массив целых чисел
- `числа` — имя переменной
- `{1, 2, 3, 4, 5}` — начальные значения

#### 🔍 ШАГ 2: Создай цикл для перебора

**Алгоритм:**

```java
for (int i = 0; i < числа.length; i++) {
}
```

**Объяснение:**
- `i = 0` — начинаем с первого элемента (индекс 0)
- `i < числа.length` — продолжаем пока i меньше длины (0, 1, 2, 3, 4)
- `числа.length` — длина массива (5)

**Важно:** Используй `<`, а не `<=`, иначе будет выход за границы!

#### 🔍 ШАГ 3: Выведи элементы

**Алгоритм:**

```java
int[] числа = {1, 2, 3, 4, 5};
for (int i = 0; i < числа.length; i++) {
    System.out.println("Элемент " + i + ": " + числа[i]);
}
```

**Объяснение:**
- `числа[i]` — доступ к элементу по индексу `i`
- На каждой итерации выводится элемент с индексом `i`

#### ✍️ ШАГ 4: Полный код

```java
public class zad6_1 {
    public static void main(String[] args) {
        int[] числа = {1, 2, 3, 4, 5};
        
        for (int i = 0; i < числа.length; i++) {
            System.out.println("Элемент " + i + ": " + числа[i]);
        }
    }
}
```

#### ⚠️ Типовые ошибки

1. **Ошибка: "ArrayIndexOutOfBoundsException"**
   - **Причина:** `i <= числа.length` (выход за границы: индекс 5 не существует)
   - **Решение:** Используй `i < числа.length`

2. **Ошибка: "cannot find symbol: variable числа"**
   - **Причина:** Массив объявлен внутри цикла или после использования
   - **Решение:** Объявляй массив ДО цикла

3. **Ошибка: выводит только последний элемент**
   - **Причина:** `System.out.println(числа[4])` вне цикла
   - **Решение:** Вывод должен быть ВНУТРИ цикла

#### ✅ Чек-лист самопроверки

- [ ] Массив создан: `int[] числа = {1, 2, 3, 4, 5};`
- [ ] Цикл: `for (int i = 0; i < числа.length; i++)`
- [ ] Используется `числа[i]` для доступа к элементу
- [ ] Выводятся все 5 элементов
- [ ] Индексы от 0 до 4
- [ ] Программа компилируется и запускается

---

### 📝 ЗАДАНИЕ 6.2: Сумма элементов массива

#### 🎯 Цель задания
Создать массив из 5 чисел. Найти и вывести сумму всех элементов.

#### 📖 Что нужно понять

**Накопление суммы в массиве:**
- Та же логика, что и в задаче 4.4
- Но теперь перебираем элементы массива, а не числа от 1 до 100

**Паттерн:**
```java
int сумма = 0;
for (int i = 0; i < массив.length; i++) {
    сумма += массив[i];
}
```

#### 🔍 ШАГ 1: Создай массив

**Алгоритм:**

```java
int[] числа = {1, 2, 3, 4, 5};
```

#### 🔍 ШАГ 2: Создай переменную для суммы

**Алгоритм:**

```java
int сумма = 0;
```

#### 🔍 ШАГ 3: Перебери массив и накапливай сумму

**Алгоритм:**

```java
int[] числа = {1, 2, 3, 4, 5};
int сумма = 0;

for (int i = 0; i < числа.length; i++) {
    сумма += числа[i];
}
```

**Объяснение:**
- На каждой итерации берем элемент `числа[i]` и добавляем к сумме
- Итерация 0: `сумма = 0 + 1 = 1`
- Итерация 1: `сумма = 1 + 2 = 3`
- Итерация 2: `сумма = 3 + 3 = 6`
- Итерация 3: `сумма = 6 + 4 = 10`
- Итерация 4: `сумма = 10 + 5 = 15`

#### 🔍 ШАГ 4: Выведи результат

**Алгоритм:**

```java
int[] числа = {1, 2, 3, 4, 5};
int сумма = 0;

for (int i = 0; i < числа.length; i++) {
    сумма += числа[i];
}

System.out.println("Сумма элементов: " + сумма);
```

#### ✍️ ШАГ 5: Полный код

```java
public class zad6_2 {
    public static void main(String[] args) {
        int[] числа = {1, 2, 3, 4, 5};
        int сумма = 0;
        
        for (int i = 0; i < числа.length; i++) {
            сумма += числа[i];
        }
        
        System.out.println("Сумма элементов: " + сумма);
    }
}
```

#### ⚠️ Типовые ошибки

1. **Ошибка: сумма всегда 0**
   - **Причина:** `сумма` объявлена внутри цикла
   - **Решение:** Объявляй `сумма` ДО цикла

2. **Ошибка: "cannot find symbol: variable сумма"**
   - **Причина:** Переменная объявлена после использования
   - **Решение:** Объявляй переменные перед использованием

#### ✅ Чек-лист самопроверки

- [ ] Массив создан
- [ ] Переменная `сумма` создана ДО цикла
- [ ] Цикл перебирает все элементы
- [ ] На каждой итерации: `сумма += числа[i]`
- [ ] Вывод ПОСЛЕ цикла
- [ ] Результат: 15 (для массива {1,2,3,4,5})
- [ ] Программа компилируется и запускается

---

## 8) Уровень 7: Методы (функции)

### 📝 ЗАДАНИЕ 7.1: Метод приветствие()

#### 🎯 Цель задания
Создать метод `приветствие()`, который выводит "Привет!". Вызвать его из main.

#### 📖 Что нужно понять

**Что такое метод:**
- Именованный блок кода
- Можно вызывать многократно
- Помогает организовать код

**Синтаксис метода:**
```java
модификатор типВозврата имяМетода() {
    // код
}
```

**Для void метода:**
```java
public static void имяМетода() {
    // код
}
```

#### 🔍 ШАГ 1: Создай базовую структуру класса

**Алгоритм:**

```java
public class zad7_1 {
    public static void main(String[] args) {
    }
}
```

#### 🔍 ШАГ 2: Создай метод приветствие()

**Алгоритм:**

```java
public class zad7_1 {
    public static void main(String[] args) {
    }
    
    public static void приветствие() {
        System.out.println("Привет!");
    }
}
```

**Объяснение:**
- `public` — метод доступен извне
- `static` — метод класса (можно вызвать без объекта)
- `void` — метод ничего не возвращает
- `приветствие()` — имя метода, без параметров

#### 🔍 ШАГ 3: Вызови метод из main

**Алгоритм:**

```java
public class zad7_1 {
    public static void main(String[] args) {
        приветствие();  // вызов метода
    }
    
    public static void приветствие() {
        System.out.println("Привет!");
    }
}
```

**Объяснение:**
- `приветствие();` — вызов метода
- Скобки `()` обязательны даже если параметров нет
- Метод выполнится, когда программа дойдет до этой строки

#### ✍️ ШАГ 4: Полный код

```java
public class zad7_1 {
    public static void main(String[] args) {
        приветствие();
    }
    
    public static void приветствие() {
        System.out.println("Привет!");
    }
}
```

#### ⚠️ Типовые ошибки

1. **Ошибка: "cannot find symbol: method приветствие()"**
   - **Причина:** Метод объявлен после `main` и компилятор не видит его (в старых версиях Java)
   - **Решение:** Порядок методов не важен, но проверь правильность имени

2. **Ошибка: "method приветствие() is already defined"**
   - **Причина:** Метод объявлен дважды
   - **Решение:** Удали дубликат

3. **Ошибка: метод не вызывается**
   - **Причина:** Забыт вызов `приветствие();` в main
   - **Решение:** Метод нужно явно вызвать

#### ✅ Чек-лист самопроверки

- [ ] Метод `приветствие()` создан
- [ ] Метод имеет правильную сигнатуру: `public static void приветствие()`
- [ ] Метод выводит "Привет!"
- [ ] Метод вызван из `main`
- [ ] Программа выводит "Привет!"
- [ ] Программа компилируется и запускается

---

### 📝 ЗАДАНИЕ 7.3: Метод сумма(int a, int b)

#### 🎯 Цель задания
Создать метод `сумма(int a, int b)`, который возвращает сумму двух чисел. Вызвать его и вывести результат.

#### 📖 Что нужно понять

**Метод с возвращаемым значением:**
- Вместо `void` указываем тип возвращаемого значения
- Используем `return` для возврата значения

**Синтаксис:**
```java
public static int сумма(int a, int b) {
    return a + b;
}
```

**Вызов метода:**
```java
int результат = сумма(5, 3);  // результат = 8
```

#### 🔍 ШАГ 1: Создай метод с параметрами

**Алгоритм:**

```java
public static int сумма(int a, int b) {
    return a + b;
}
```

**Объяснение:**
- `int` — тип возвращаемого значения (не `void`!)
- `int a, int b` — параметры метода
- `return a + b;` — возвращает сумму

#### 🔍 ШАГ 2: Вызови метод и сохрани результат

**Алгоритм:**

```java
public class zad7_3 {
    public static void main(String[] args) {
        int результат = сумма(5, 3);
        System.out.println("Сумма: " + результат);
    }
    
    public static int сумма(int a, int b) {
        return a + b;
    }
}
```

**Объяснение:**
- `сумма(5, 3)` — вызов метода с аргументами 5 и 3
- `int результат = ...` — сохранение возвращаемого значения
- `return` завершает метод и возвращает значение

#### ✍️ ШАГ 3: Полный код

```java
public class zad7_3 {
    public static void main(String[] args) {
        int результат = сумма(5, 3);
        System.out.println("Сумма: " + результат);
    }
    
    public static int сумма(int a, int b) {
        return a + b;
    }
}
```

#### ⚠️ Типовые ошибки

1. **Ошибка: "missing return statement"**
   - **Причина:** Метод объявлен как `int`, но нет `return`
   - **Решение:** Все пути выполнения должны иметь `return`

2. **Ошибка: "incompatible types: void cannot be converted to int"**
   - **Причина:** Метод объявлен как `void`, но пытаешься сохранить результат
   - **Решение:** Измени `void` на `int`

3. **Ошибка: метод не вызывается**
   - **Причина:** Забыт вызов метода
   - **Решение:** Вызови метод: `сумма(5, 3);`

#### ✅ Чек-лист самопроверки

- [ ] Метод имеет тип возврата `int` (не `void`)
- [ ] Метод принимает два параметра: `int a, int b`
- [ ] Метод возвращает `a + b`
- [ ] Метод вызван из `main`
- [ ] Результат сохранен в переменную
- [ ] Результат выведен на экран
- [ ] Программа компилируется и запускается

---

## 9) Мини-набор советов "как проверять себя быстро"

### Проверка синтаксиса

- **Компиляция:** `javac файл.java` — если есть ошибки, исправь их
- **Запуск:** `java файл.java` — если работает, синтаксис правильный

### Проверка логики

- **Тестируй граничные случаи:**
  - Для условий: `age = 17, 18, 19` (граница 18)
  - Для циклов: пустой массив, массив из 1 элемента
  - Для массивов: первый и последний элемент

### Проверка вывода

- **Сверяй с примером:** Вывод должен точно совпадать с примером из задания
- **Проверяй форматирование:** Пробелы, переносы строк, знаки препинания

### Типовые проблемы

- **"cannot find symbol"** → переменная/метод не объявлен или не в области видимости
- **"incompatible types"** → неправильный тип данных
- **"ArrayIndexOutOfBoundsException"** → выход за границы массива
- **Бесконечный цикл** → условие никогда не становится `false`

---

## 10) Итого

Ты изучил ключевые концепции Java:

- переменные и типы данных,
- условные операторы и циклы,
- массивы и методы,
- классы и объекты,
- наследование и полиморфизм,
- коллекции и обработка исключений.

**Помни:**
- Практика важнее теории — пиши код!
- Не бойся ошибок — они учат
- Читай сообщения об ошибках — они подсказывают, что не так
- Комментируй код — это поможет понять логику позже

Удачи в изучении Java! 🚀

