# 📚 Полное руководство по индексам в PostgreSQL

## 🎯 ЧАСТЬ 1: ТЕОРИЯ - ЧТО ТАКОЕ ИНДЕКСЫ?

### Что такое индекс простыми словами?

**Индекс = оглавление в книге**

Представь толстую книгу без оглавления:
- Чтобы найти главу про "SQL", нужно листать ВСЮ книгу
- Это ОЧЕНЬ медленно

Теперь представь ту же книгу С оглавлением:
- Смотришь в оглавление → видишь "SQL, страница 45"
- Открываешь сразу страницу 45
- Это БЫСТРО!

**В базе данных то же самое:**
- **Без индекса**: PostgreSQL читает ВСЕ строки таблицы подряд (это называется "Seq Scan" - последовательное сканирование)
- **С индексом**: PostgreSQL смотрит в индекс и сразу находит нужные строки (это называется "Index Scan" - сканирование по индексу)

### Зачем нужны индексы?

1. **Ускорение поиска** - найти данные в 1000 раз быстрее
2. **Ускорение JOIN** - объединение таблиц работает быстрее
3. **Ускорение сортировки** - данные уже частично отсортированы
4. **Проверка уникальности** - гарантировать, что данные не повторяются

### Типы индексов в этом проекте:

#### 1. **Обычный индекс (B-Tree)**
```sql
CREATE INDEX idx_name ON table_name (column_name);
```
- Самый простой тип
- Ускоряет поиск по одному столбцу
- Используется по умолчанию в PostgreSQL

#### 2. **Функциональный индекс**
```sql
CREATE INDEX idx_name ON table_name (UPPER(column_name));
```
- Индекс на ВЫРАЖЕНИЕ (функцию), а не на столбец
- Полезен для поиска без учета регистра
- **ВАЖНО**: работает только если в запросе используется ТО ЖЕ выражение

#### 3. **Многоколоночный индекс**
```sql
CREATE INDEX idx_name ON table_name (col1, col2, col3);
```
- Индекс на НЕСКОЛЬКО столбцов сразу
- Порядок столбцов ВАЖЕН!
- Работает для запросов с условиями на первые столбцы

#### 4. **Уникальный индекс**
```sql
CREATE UNIQUE INDEX idx_name ON table_name (col1, col2);
```
- Гарантирует, что комбинация значений уникальна
- Нельзя вставить дубликаты

#### 5. **Частичный индекс (Partial Index)**
```sql
CREATE INDEX idx_name ON table_name (col1, col2) WHERE condition;
```
- Индекс только для строк, которые соответствуют условию
- Экономит место и работает быстрее

### Как проверить, что индекс работает?

Используй команду **EXPLAIN ANALYZE**:

```sql
EXPLAIN ANALYZE
SELECT * FROM table_name WHERE column = 'value';
```

**Что смотреть в выводе:**
- ✅ `Index Scan using idx_name` - индекс ИСПОЛЬЗУЕТСЯ (хорошо!)
- ❌ `Seq Scan on table_name` - индекс НЕ используется (плохо!)

**Проблема**: Для маленьких таблиц PostgreSQL может выбрать Seq Scan, потому что это быстрее.

**Решение**: Используй `SET enable_seqscan = OFF;` перед запросом (только для демонстрации!)

---

## 🎯 ЧАСТЬ 2: СТРУКТУРА БАЗЫ ДАННЫХ

Перед началом работы нужно понять структуру:

### Таблицы:

1. **person** - люди (клиенты)
   - id, name, age, gender, address

2. **pizzeria** - пиццерии
   - id, name, rating

3. **menu** - меню (пиццы в пиццериях)
   - id, pizzeria_id (→ pizzeria.id), pizza_name, price

4. **person_visits** - посещения пиццерий
   - id, person_id (→ person.id), pizzeria_id (→ pizzeria.id), visit_date

5. **person_order** - заказы
   - id, person_id (→ person.id), menu_id (→ menu.id), order_date

### Внешние ключи (Foreign Keys):
- `menu.pizzeria_id` → `pizzeria.id`
- `person_visits.person_id` → `person.id`
- `person_visits.pizzeria_id` → `pizzeria.id`
- `person_order.person_id` → `person.id`
- `person_order.menu_id` → `menu.id`

---

## 🎯 ЧАСТЬ 3: ПОШАГОВОЕ ВЫПОЛНЕНИЕ ЗАДАНИЙ

### 📝 ЗАДАНИЕ 00: Создать индексы для всех внешних ключей

**Что нужно сделать:**
Создать простой B-Tree индекс для каждого внешнего ключа.

**Правило именования:** `idx_{имя_таблицы}_{имя_столбца}`

**Шаги:**

1. Открой файл `src/ex00/day05_ex00.sql`

2. Найди все внешние ключи:
   - `menu.pizzeria_id`
   - `person_visits.person_id`
   - `person_visits.pizzeria_id`
   - `person_order.person_id`
   - `person_order.menu_id`

3. Для каждого создай индекс:
```sql
CREATE INDEX idx_menu_pizzeria_id ON menu (pizzeria_id);
CREATE INDEX idx_person_visits_person_id ON person_visits (person_id);
CREATE INDEX idx_person_visits_pizzeria_id ON person_visits (pizzeria_id);
CREATE INDEX idx_person_order_person_id ON person_order (person_id);
CREATE INDEX idx_person_order_menu_id ON person_order (menu_id);
```

**Проверка:**
```sql
SELECT indexname FROM pg_indexes WHERE schemaname = 'public' AND indexname LIKE 'idx_%';
```
Должно быть 5 индексов.

---

### 📝 ЗАДАНИЕ 01: Показать, что индекс работает

**Что нужно сделать:**
1. Написать запрос, который возвращает названия пицц и названия пиццерий
2. Доказать через EXPLAIN ANALYZE, что индекс используется

**Шаги:**

1. Открой файл `src/ex01/day05_ex01.sql`

2. Напиши запрос с JOIN:
```sql
SELECT m.pizza_name, pz.name AS pizzeria_name
FROM menu m
INNER JOIN pizzeria pz ON m.pizzeria_id = pz.id;
```

3. Добавь команды для проверки:
```sql
SET enable_seqscan = OFF;  -- Запрещаем Seq Scan

EXPLAIN ANALYZE 
SELECT m.pizza_name, pz.name AS pizzeria_name
FROM menu m
INNER JOIN pizzeria pz ON m.pizzeria_id = pz.id;
```

**Что должно быть в выводе:**
```
Index Scan using idx_menu_pizzeria_id on menu m
```

**Почему нужен SET enable_seqscan = OFF?**
Для маленьких таблиц PostgreSQL может выбрать Seq Scan как более быстрый вариант. Эта команда заставляет использовать индекс.

---

### 📝 ЗАДАНИЕ 02: Функциональный индекс

**Что нужно сделать:**
Создать функциональный индекс на `UPPER(name)` для таблицы `person`.

**Шаги:**

1. Открой файл `src/ex02/day05_ex02.sql`

2. Создай функциональный индекс:
```sql
CREATE INDEX idx_person_name ON person (UPPER(name));
```

3. Напиши запрос, который использует ТО ЖЕ выражение:
```sql
SET enable_seqscan = OFF;

EXPLAIN ANALYZE
SELECT name
FROM person
WHERE UPPER(name) = 'ANNA';
```

**ВАЖНО:** 
- В индексе: `UPPER(name)`
- В запросе: `WHERE UPPER(name) = 'ANNA'` ✅
- Если написать `WHERE name = 'Anna'` ❌ - индекс НЕ сработает!

**Что должно быть в выводе:**
```
Index Scan using idx_person_name on person
  Index Cond: (upper(name) = 'ANNA'::text)
```

---

### 📝 ЗАДАНИЕ 03: Многоколоночный индекс

**Что нужно сделать:**
Создать многоколоночный индекс для запроса:
```sql
SELECT person_id, menu_id, order_date
FROM person_order
WHERE person_id = 8 AND menu_id = 19;
```

**Шаги:**

1. Открой файл `src/ex03/day05_ex03.sql`

2. Создай многоколоночный индекс:
```sql
CREATE INDEX idx_person_order_multi ON person_order (person_id, menu_id, order_date);
```

3. Напиши запрос с EXPLAIN ANALYZE:
```sql
SET enable_seqscan = OFF;

EXPLAIN ANALYZE
SELECT person_id, menu_id, order_date
FROM person_order
WHERE person_id = 8 AND menu_id = 19;
```

**Что должно быть в выводе:**
```
Index Only Scan using idx_person_order_multi on person_order
```

**Что такое Index Only Scan?**
Это когда PostgreSQL может получить ВСЕ нужные данные из индекса, не обращаясь к самой таблице. Это САМЫЙ БЫСТРЫЙ способ!

**Почему это работает?**
Индекс содержит `person_id, menu_id, order_date` - все столбцы, которые нужны в SELECT!

---

### 📝 ЗАДАНИЕ 04: Уникальный индекс

**Что нужно сделать:**
Создать уникальный индекс на `pizzeria_id` и `pizza_name` в таблице `menu`.

**Зачем?**
Чтобы в одной пиццерии не было двух пицц с одинаковым названием.

**Шаги:**

1. Открой файл `src/ex04/day05_ex04.sql`

2. Создай уникальный индекс:
```sql
CREATE UNIQUE INDEX idx_menu_unique ON menu (pizzeria_id, pizza_name);
```

3. Напиши запрос для проверки:
```sql
SET enable_seqscan = OFF;

EXPLAIN ANALYZE
SELECT pizzeria_id, pizza_name, price
FROM menu
WHERE pizzeria_id = 1 AND pizza_name = 'cheese pizza';
```

**Что должно быть в выводе:**
```
Index Scan using idx_menu_unique on menu
```

**Проверка уникальности:**
Попробуй вставить дубликат:
```sql
INSERT INTO menu (pizzeria_id, pizza_name, price) VALUES (1, 'cheese pizza', 1000);
```
Должна быть ошибка - уникальный индекс не позволит!

---

### 📝 ЗАДАНИЕ 05: Частичный уникальный индекс

**Что нужно сделать:**
Создать частично уникальный индекс для `person_id` и `menu_id`, но только для даты '2022-01-01'.

**Зачем?**
Гарантировать, что один человек не может заказать одну и ту же пиццу дважды в один день.

**Шаги:**

1. Открой файл `src/ex05/day05_ex05.sql`

2. Создай частичный уникальный индекс:
```sql
CREATE UNIQUE INDEX idx_person_order_order_date 
ON person_order (person_id, menu_id, order_date) 
WHERE order_date = '2022-01-01';
```

3. Напиши запрос:
```sql
SET enable_seqscan = OFF;

EXPLAIN ANALYZE
SELECT person_id, menu_id, order_date
FROM person_order
WHERE order_date = '2022-01-01' AND person_id = 1 AND menu_id = 1;
```

**Что должно быть в выводе:**
```
Index Only Scan using idx_person_order_order_date on person_order
```

**Важно:**
- Индекс работает ТОЛЬКО для строк с `order_date = '2022-01-01'`
- Для других дат этот индекс не используется
- Экономит место и работает быстрее

---

### 📝 ЗАДАНИЕ 06: Оптимизация производительности

**Что нужно сделать:**
Проанализировать запрос и создать индекс, который улучшит время выполнения.

**Исходный запрос:**
```sql
SELECT
    m.pizza_name AS pizza_name,
    max(rating) OVER (PARTITION BY rating ORDER BY rating ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING) AS k
FROM  menu m
INNER JOIN pizzeria pz ON m.pizzeria_id = pz.id
ORDER BY 1,2;
```

**Шаги:**

1. Открой файл `src/ex06/day05_ex06.sql`

2. Запусти EXPLAIN ANALYZE БЕЗ индекса:
```sql
EXPLAIN ANALYZE
SELECT
    m.pizza_name AS pizza_name,
    max(rating) OVER (PARTITION BY rating ORDER BY rating ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING) AS k
FROM  menu m
INNER JOIN pizzeria pz ON m.pizzeria_id = pz.id
ORDER BY 1,2;
```
Запиши Execution Time (например, 0.338 ms)

3. Создай индекс (методом проб и ошибок):
```sql
CREATE INDEX idx_1 ON pizzeria (rating, id);
```

4. Запусти EXPLAIN ANALYZE С индексом:
```sql
SET enable_seqscan = OFF;

EXPLAIN ANALYZE
SELECT
    m.pizza_name AS pizza_name,
    max(rating) OVER (PARTITION BY rating ORDER BY rating ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING) AS k
FROM  menu m
INNER JOIN pizzeria pz ON m.pizzeria_id = pz.id
ORDER BY 1,2;
```

5. Сравни Execution Time - должно быть меньше!

**Подсказка:**
- Смотри на план выполнения - что там самое медленное?
- Запрос использует `rating` для сортировки и оконных функций
- JOIN идет по `pizzeria_id = pz.id`
- Попробуй создать индекс на `pizzeria` с `rating` и `id`

**Если не работает:**
- Удали индекс: `DROP INDEX idx_1;`
- Попробуй другой вариант
- Повторяй до улучшения

---

## 🎯 ЧАСТЬ 4: КАК ДЕЛАТЬ ПРОЕКТЫ САМОСТОЯТЕЛЬНО

### Подготовка:

1. **Установи PostgreSQL** (если еще не установлен)

2. **Создай базу данных:**
```sql
CREATE DATABASE your_database_name;
```

3. **Примени модель базы данных:**
   - Открой файл `materials/model.sql`
   - Выполни весь скрипт в своей базе данных
   - Проверь, что таблицы созданы

4. **Проверь структуру:**
```sql
\dt  -- Показать все таблицы
\d person  -- Показать структуру таблицы person
```

### Алгоритм работы над каждым заданием:

1. **Прочитай задание внимательно**
   - Что нужно создать?
   - Какое имя должно быть у индекса?
   - Какие столбцы нужны?

2. **Изучи структуру таблиц**
   - Какие столбцы есть?
   - Какие внешние ключи?
   - Какие данные хранятся?

3. **Напиши CREATE INDEX**
   - Проверь синтаксис
   - Проверь имя индекса
   - Проверь столбцы

4. **Создай тестовый запрос**
   - Запрос должен использовать индекс
   - Используй EXPLAIN ANALYZE

5. **Проверь результат**
   - Видно ли `Index Scan` в выводе?
   - Если нет - используй `SET enable_seqscan = OFF;`

6. **Сохрани в файл**
   - Файл должен быть в правильной папке (ex00, ex01, и т.д.)
   - Имя файла должно быть правильным (day05_ex00.sql, и т.д.)

### Полезные команды:

```sql
-- Показать все индексы
SELECT indexname, tablename, indexdef 
FROM pg_indexes 
WHERE schemaname = 'public';

-- Удалить индекс
DROP INDEX idx_name;

-- Проверить план выполнения
EXPLAIN ANALYZE SELECT ...;

-- Отключить Seq Scan (для тестирования)
SET enable_seqscan = OFF;

-- Включить Seq Scan обратно
SET enable_seqscan = ON;
```

### Частые ошибки:

1. **Неправильное имя индекса**
   - Должно быть: `idx_{table}_{column}`
   - Проверь в задании!

2. **Индекс не используется**
   - Используй `SET enable_seqscan = OFF;`
   - Проверь, что в запросе используется то же выражение, что и в индексе

3. **Функциональный индекс не работает**
   - В запросе должно быть ТО ЖЕ выражение (например, `UPPER(name)`)

4. **Многоколоночный индекс не работает**
   - Порядок столбцов важен!
   - Индекс работает для условий на первые столбцы

5. **Index Only Scan не получается**
   - В SELECT должны быть ТОЛЬКО столбцы из индекса
   - Не добавляй лишние столбцы!

### Как проверить решение:

1. Выполни все CREATE INDEX команды
2. Выполни EXPLAIN ANALYZE запросы
3. Проверь, что видно `Index Scan` или `Index Only Scan`
4. Убедись, что Execution Time улучшился (для задания 06)

---

## 🎯 ИТОГОВАЯ ШПАРГАЛКА

### Типы индексов:

```sql
-- Обычный
CREATE INDEX idx_name ON table (column);

-- Функциональный
CREATE INDEX idx_name ON table (UPPER(column));

-- Многоколоночный
CREATE INDEX idx_name ON table (col1, col2, col3);

-- Уникальный
CREATE UNIQUE INDEX idx_name ON table (col1, col2);

-- Частичный
CREATE INDEX idx_name ON table (col1) WHERE condition;
```

### Проверка работы:

```sql
SET enable_seqscan = OFF;
EXPLAIN ANALYZE SELECT ...;
```

### Что искать в выводе:

- ✅ `Index Scan using idx_name` - хорошо!
- ✅ `Index Only Scan using idx_name` - отлично!
- ❌ `Seq Scan on table` - плохо (индекс не используется)

---

**Удачи в выполнении проекта! 🚀**

